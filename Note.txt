Row\Column:
表示组件的排列方向。
Row：    1\2\3\.....  类似"horization"
Column:  1            类似"vertical"
         2
         3
         .....

填充：EdgeInsets

Container:
    对子widget进行 绘制(painting)、定位(positioning)、调整大小(sizing)操作
    可以根据几个属性，判断其能力：
    1. Alignment：约束child的位置
    2. Constraints：控件占用的空间大小
    3. Margin：给Container设置"外"边距
    4. Padding 设置Container内边距

所有布局widget都有一个
"child"属性（例如Center或Container）
"children"属性对应的是widget列表（例如Row，Column，ListView或Stack）

对齐：MainAxisAlignment、CrossAxisAlignment
    需要弄清一个概念，row(行)、column(列)的主轴、副轴是不一样的！
    row 的 mainAxis 是横向；crossAxis 是纵向
    column 的 mainAxis 是纵向；crossAxis 是横向

setState 流程：
// 标记"脏"
1. setState
2. _element.markNeedsBuild()
3. owner.scheduleBuildFor(this);
4. _dirtyElements.add(element);
5. .....

// "当[WidgetsBinding.drawFrame]中调用 buildScope 的时候能够重构 element"
TODO：还不能理解这个流程!

参考：
[Flutter setState做了些什么事情](https://www.jianshu.com/p/a727ab7948e0)
[Flutter运行机制-从启动到显示](https://book.flutterchina.club/chapter14/flutter_app_startup.html)

结论：
setState 调用时会自己添加到 BuildOwner 的_dirtyElements 脏链表中，然后调用 window.scheduleFrame()，最终等待更新帧信号的来临从而刷新需要重构的界面。


